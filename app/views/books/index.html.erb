<h1>Books</h1>
<% @books.each do |list| %>
 <p>title</p>
 <span><%= book.title %></span>
<% end %>
<table>
 <thead>
  <tr>
   <th>Title</th>
   <th>Body</th>
   <th>null</th>
  </tr>
 </thead>
</table>


<h2>New books</h2>
<%= form_with model: @book, url:'/books', local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>
